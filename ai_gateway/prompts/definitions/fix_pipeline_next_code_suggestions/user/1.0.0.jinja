The failing pipeline URL is:
<pipeline_url>{{pipeline_url}}</pipeline_url>

The merge request to post suggestions on:
<merge_request_url>{{merge_request_url}}</merge_request_url>

A prior execution step has already applied the fix to the working tree. Use `git diff` to see the
changes, and post them as inline code suggestions on the merge request. Here is a summary of the
execution step for additional context:
<execution_results>
  {{execution_results}}
</execution_results>
