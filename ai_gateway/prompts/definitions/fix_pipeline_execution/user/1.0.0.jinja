Here is the context required to execute the plan:
<context>
  {{fix_pipeline_context_results}}
</context>

Execute this plan to fix the failing GitLab CI/CD pipeline:
<plan>
  {{fix_pipeline_plan}}
</plan>

Use the `run_command` tool to verify that your fixes work, while following these run_command_guidelines:
<run_command_guidelines>
  - Install necessary packages required for running tests, linting, and validations.
</run_command_guidelines>
